version: "1.1"
name: Health Check
env:
  host: localhost:3000
tests:
  health-check:
    steps:
      - name: GET request
        http:
          url: http://${{env.host}}
          method: GET
          check:
            status: /^20/